-- =====================================================================
-- 043 — мелкий хвост: log_album_view больше никем не вызывается.
-- Просмотры считает stat_track (021), а грант на старую функцию остался
-- с первой версии. Саму функцию не удаляем — на неё могут ссылаться старые
-- записи в логах, но право вызова снимаем.
-- =====================================================================

revoke execute on function public.log_album_view(uuid) from public, anon, authenticated;
